import fs from 'node:fs';import path from 'node:path';const root=new URL('..',import.meta.url).pathname;const required=['index.html','styles.css','app.js','admin.html','admin.css','api/health.js','api/advisor.js','api/model-policy.js','lib/policy.js','lib/calculators.js','lib/advisor.js','vercel.json','assets/noeprax-logo.svg'];let failures=[];for(const f of required){const p=path.join(root,f);if(!fs.existsSync(p)||fs.statSync(p).size===0)failures.push(`missing/empty ${f}`)}const html=fs.readFileSync(path.join(root,'index.html'),'utf8'),css=fs.readFileSync(path.join(root,'styles.css'),'utf8');for(const marker of ['id="navigator"','id="calculators"','id="pricing"','id="governance"','data-i18n','/assets/noeprax-logo.svg'])if(!html.includes(marker))failures.push(`index missing ${marker}`);for(const bad of ['sk-proj-','service_role','SUPABASE_SERVICE_ROLE_KEY=','OPENAI_API_KEY=','DEEPSEEK_API_KEY='])if(html.includes(bad)||css.includes(bad))failures.push(`secret-like literal detected: ${bad}`);if(!css.includes('@media(max-width:620px)'))failures.push('narrow mobile breakpoint missing');if(!css.includes('prefers-reduced-motion'))failures.push('reduced motion handling missing');if(failures.length){console.error('VERIFY FAILED\n'+failures.join('\n'));process.exit(1)}console.log('VERIFY GREEN: required files, responsive contract, basic secret/content checks pass.');
