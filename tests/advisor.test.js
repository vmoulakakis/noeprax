import test from 'node:test';import assert from 'node:assert/strict';import {advise} from '../lib/advisor.js';test('real estate gets vertical agents',()=>{const r=advise({industry:'real_estate',hours:20,leads:100,integrations:'some'});assert.equal(r.industry.en,'Real Estate');assert.ok(r.agents.includes('AI Lead Qualifier'));assert.equal(r.complexity,'Medium')});test('unknown industry falls back safely',()=>{const r=advise({industry:'x',hours:0,leads:0});assert.equal(r.industry.en,'Other business');assert.ok(r.opportunityScore>=45)});
