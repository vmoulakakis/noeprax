import {advise} from '../lib/advisor.js';import {selectModel} from '../lib/policy.js';
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'method_not_allowed'});const input=typeof req.body==='string'?JSON.parse(req.body||'{}'):(req.body||{});const freeProviderAvailable=Boolean(process.env.OPENROUTER_API_KEY||process.env.GEMINI_API_KEY);const selected=selectModel({entitlement:'free',complexity:1,sensitive:Boolean(input.sensitive),freeProviderAvailable});const result=advise(input);return res.status(200).json({mode:selected.id==='deterministic'?'zero-token-deterministic':'free-model-eligible',modelPolicy:selected.id,...result})}
