import {selectModel,enforcePaidTokenGate} from '../lib/policy.js';
export default function handler(req,res){const entitlement=req.headers['x-noeprax-entitlement']==='paid'?'paid':'free';const complexity=Math.max(1,Math.min(5,Number(req.query?.complexity||1)));const sensitive=String(req.query?.sensitive||'false')==='true';const freeProviderAvailable=Boolean(process.env.OPENROUTER_API_KEY||process.env.GEMINI_API_KEY);const model=selectModel({entitlement,complexity,sensitive,freeProviderAvailable});if(!enforcePaidTokenGate({entitlement,model}))return res.status(403).json({error:'paid_model_blocked_for_free_entitlement'});return res.status(200).json({entitlement,selected:model.id,billing:model.billing,sensitive,rule:'free visitors never incur paid model tokens'})}
